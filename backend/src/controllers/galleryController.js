export const getImages = (req, res) => {
  res.send("Get admin");
};

export const uploadImage = (req, res) => {
  //Implement logic to handle image upload, save it to the database, and send a response
};

export const deleteImage = (req, res) => {
  //Implement logic to delete an image from the database based on the provided image ID and send a response
};
