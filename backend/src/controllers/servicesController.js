export const getServices = (req, res) => {
  res.send("Get admin");
};
export const addService = (res, req) => {
  //Implement logic to add a new service to the database based on the provided service data and send a response
};
export const updateService = (res, req) => {
  //Implement logic to update an existing service in the database based on the provided service ID and updated data, then send a response
};
export const deleteService = (res, req) => {
  //Implement logic to delete a service from the database based on the provided service ID and send a response
};
